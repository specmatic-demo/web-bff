name: Specmatic Contract Tests

on:
  push:
    branches:
      - main
  schedule:
    - cron: "10 0 * * *"
  workflow_dispatch:

jobs:
  contract-tests:
    uses: specmatic-demo/.github/.github/workflows/specmatic-reports.yml@main
    secrets: inherit
